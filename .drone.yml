workspace:
  base: /srv/drone-demo
  path: .

pipeline:
  build:
     image: golang
     # pull: true
     commands:
       - pwd
       - ls
       - go get -u github.com/aliyun/alibaba-cloud-sdk-go/sdk
       - go build alidns.go
